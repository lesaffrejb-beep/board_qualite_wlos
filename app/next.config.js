/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ydfgueqasslzhdbvermu.supabase.co'],
    },
    swcMinify: true,
    compress: true,
}

module.exports = nextConfig
